# ── Core web framework ──────────────────────────────────────────────
streamlit>=1.32.0

# ── ML / Clinical model ─────────────────────────────────────────────
scikit-learn>=1.3.0
joblib>=1.3.0
xgboost>=1.7.0          # Required — clinical model uses XGBoost in stacking ensemble

# ── CNN / Deep learning ──────────────────────────────────────────────
# IMPORTANT: Your CNN was saved with Keras 3.13.2 (standalone Keras 3).
# Do NOT use tensorflow==2.13 — that ships keras 2.x which CANNOT load
# .keras files saved with Keras 3.
#
# Option A (recommended — CPU-only, smaller install):
keras>=3.0.0
tensorflow>=2.16.0      # TF 2.16+ bundles Keras 3 by default

# Option B — if on Apple Silicon (M1/M2/M3), replace the two lines above with:
# keras>=3.0.0
# tensorflow-macos>=2.16.0
# tensorflow-metal        # GPU acceleration on Apple Silicon

# ── Data / numerics ──────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0

# ── Image processing ────────────────────────────────────────────────
Pillow>=10.0.0

# ── Visualisation ───────────────────────────────────────────────────
matplotlib>=3.7.0

# ── XAI (optional — only needed for SHAP explanations) ──────────────
shap>=0.44.0
